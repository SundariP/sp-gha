name: Hello World

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  # This workflow contains a single job called "hello" or any randam name
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo " A demo world"
  
   test:
    runs-on: ubuntu-latest
    steps:
      - name: test phase
        run: echo " Running test cases "
        
   deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the App
        run: echo " Deploying the app "

 
    
